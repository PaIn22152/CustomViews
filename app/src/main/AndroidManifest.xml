<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.perdev.customviews">

  <application
    android:allowBackup="true"
    android:hardwareAccelerated="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".SnowActivity">
    </activity>
    <activity android:name=".ScreenAdaptActivity">
    </activity>
    <activity android:name=".JavaTestActivity">
    </activity>
    <activity android:name=".ScrollTestActivity">
    </activity>
    <activity android:name=".BitmapFilterActivity">
    </activity>
    <activity android:name=".ColorProgressBarActivity">
    </activity>
    <activity android:name=".CornerCoverActivity">
    </activity>
    <activity android:name=".RCLayoutActivity">
    </activity>
    <activity android:name=".DialActivity">
    </activity>
    <activity android:name=".PieActivity">
    </activity>
    <activity android:name=".MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>

</manifest>